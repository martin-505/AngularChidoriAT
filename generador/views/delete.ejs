<% include _header %>
    <div class="col-md-4 mx-auto">
        <div class="card text-center">
            <div class="card-header">
                <h3>Deñete Zombie</h3>
            </div>
            <div class="card-body">
                <form action="/zombies/delete/<%= zombie.id %>?_method=DELETE" method="POST">
                    <div class="form-group">
                        <input type="text" name="nombre" class="form-control" value="<%= zombie.nombre %>" required>
                    </div>
                    <div class="form-group">
                        <input type="email" name="email" class="form-control" value="<%= zombie.email %>" required>
                    </div>
                    <div class="form-group">
                        <input type="text" name="type" class="form-control" value="<%= zombie.type %>" required>
                    </div>
                    <div class="form-group">
                        <button class="btn btn-dark"> <i class="fas fa-skull"></i> Eliminar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <% include _footer %>