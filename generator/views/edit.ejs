<% include _Header %>
    <div class="col-md-4 mx-auto">
        <div class="card text-center">
            <div class="card-header">
                <h3>Deñete Zombie</h3>
            </div>
            <div class="card-body">
                <form action="/zombie/edit/<%= zombie.id%>?_method=PUT" method="POST">
                    <div class="form-group">
                        <input type="text" name="name" class="form-control" value="<%= zombie.name %>" required>
                    </div>
                    <div class="form-group">
                        <input type="email" name="email" class="form-control" value="<%= zombie.email %>" required>
                    </div>
                    <div class="form-group">
                        <input type="text" name="type" class="form-control" value="<%= zombie.type %>" required>
                    </div>
                    <% if(text!=false){%>

                        <% if(text.name)
                    {      
                    %>

                            <div class="alert alert-<%=clas %>">
                                <%=text.name.message %>
                            </div>
                            <%
                    }
                    %>
                                <% if(text.email)
                    {      
                    %>

                                    <div class="alert alert-<%=clas %>">
                                        <%=text.email.message %>
                                    </div>
                                    <%
                    }
                    %>
                                        <% if(text.type)
                    {      
                    %>
                                            <div class="alert alert-<%=clas %>">
                                                <%=text.type.message %>
                                            </div>
                                            <%
                    }
                    %>
                                                <%}%>
                                                    <%if(mensaje.length>0){%>
                                                        <div class="alert alert-<%=clas %>">
                                                            <%=mensaje %>
                                                        </div>
                                                        <%}%>
                                                            <div class="form-group">
                                                                <button class="btn btn-dark"> Guardar</button>
                                                            </div>
                </form>
            </div>
        </div>
    </div>

    <% include _footer %>